import org.scalatest.funsuite.AnyFunSuite

class BowlingTest extends AnyFunSuite {
  test("Bowling.frames 1 frame") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 2 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 3 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 4 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 5 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 6 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 7 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 8 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.frames 9 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test ("Bowling.frames 10 frames") {
    assert(Bowling.cube(3) === 27)
  }

  test("Bowling.scores 1 frame") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 2 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 3 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 4 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 5 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 6 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 7 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 8 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 9 frames") {
    assert(Bowling.cube(3) === 27)
  }
  test("Bowling.scores 10 frames") {
    assert(Bowling.cube(3) === 27)
  }
}

//[6,2,7,2,3,4,8,2,9,0,10,10,10,6,3,4,6,10]
// [8,17,24,43,52,82,108,127,136,156]